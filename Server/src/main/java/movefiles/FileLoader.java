package movefiles;

import io.netty.buffer.ByteBuf;
import model.FileInfo;

// Класс с реализацией загрузки и считывания фалов
public class FileLoader {

    public static void loadToServer(ByteBuf buffer, String userName, FileInfo file) {

    }
}
